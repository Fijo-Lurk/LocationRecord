language: node_js
os: linux
dist: trusty
node_js:
  - '12'

before_install:
  - export CHROME_BIN=google-chrome-stable


addons:
  chrome: stable
  sources:
      - google-chrome	    
  packages:	
      - dpkg  
      - google-chrome-stable

cache:
  directories:
    - ./node_modules
    

 
install:
  - cd frontend && npm install
  
script:
  - ng test
